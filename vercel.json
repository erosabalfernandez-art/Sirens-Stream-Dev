{
  "installCommand": "npx --yes pnpm@9 install --no-frozen-lockfile --prod=false",
  "buildCommand": "npx --yes pnpm@9 --filter @workspace/api-server run build && BASE_PATH=/ npx --yes pnpm@9 --filter @workspace/sirens-stream run build",
  "outputDirectory": "artifacts/sirens-stream/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}